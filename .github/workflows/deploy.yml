name: CI/CD

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  test:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:15-alpine
        env:
          POSTGRES_PASSWORD: testpass
          POSTGRES_DB: passwords_db_test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run migrations
        env:
          PGPASSWORD: testpass
        run: |
          for f in migrations/*.sql; do
              psql -h localhost -U postgres -d passwords_db_test -f "$f"
          done
      - name: Lint
        run: flake8 src/ app.py --max-line-length=120 --ignore=E501,W503

      - name: Run tests
        env:
          DATABASE_URL: postgres://postgres:testpass@localhost:5432/passwords_db_test
          JWT_SECRET: test-jwt-secret-for-ci
          PYTHONPATH: .
        run: pytest --cov=src --cov-report=term-missing --cov-fail-under=80

        
  build-and-push:
    needs: test
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master')

    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@8d2750c68a42422c14e847fe6c8ac0403b4cbd6f  # v3

      - name: Log in to Docker Hub
        uses: docker/login-action@c94ce9fb468520275223c153574b00df6fe4bcc9  # v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push Docker image
        uses: docker/build-push-action@ca052bb54ab0790a636c9b5f226502c73d547a25  # v5
        with:
          context: .
          push: true
          tags: |
            ${{ secrets.DOCKER_USERNAME }}/secure-password-manager-api:latest
            ${{ secrets.DOCKER_USERNAME }}/secure-password-manager-api:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

